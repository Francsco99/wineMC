<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage Icon="wineicon.png"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="SommeliAr.Views.Menu.ScanPage">
    <ContentPage.Content>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="70"/>            
                <RowDefinition Height="8*"/>        
                <RowDefinition Height="2*"/>
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="1.5*"/>
            </Grid.ColumnDefinitions>
            
            <StackLayout Grid.Row="0" Grid.ColumnSpan="3" VerticalOptions="End" HorizontalOptions="Center">
                <Label Text="Let's scan bottles!"
                       TextColor="#8b52ff"
                       FontSize="Title"
                       FontAttributes="Bold"
                       />
            </StackLayout>

            <StackLayout Grid.Row="1" Grid.ColumnSpan="3" Margin="0,60,0,0">
                <Image x:Name="resultImage" Scale="1" HorizontalOptions="CenterAndExpand"/>
            </StackLayout>


            <StackLayout Grid.Row="2" Grid.Column="1" VerticalOptions="Start" HorizontalOptions="Center" Margin="0,60,0,0">
                <Button x:Name="Scan_btn" Text="Scan!"
                        TextColor="White" FontAttributes="Bold"
                        FontSize="Large"        
                        BackgroundColor="#8b52ff"
                        BorderWidth="1"
                        CornerRadius="15"
                        WidthRequest="100"
                        Clicked="Scan_btn_Clicked"/>

            </StackLayout>

            <StackLayout Grid.Row="2" Grid.Column="2" VerticalOptions="Start" HorizontalOptions="Center" Margin="0,-20,0,0">
                <Button x:Name="After_sca_btn" Text="Result list"
                        BorderColor="#8b52ff"
                        TextColor="#8b52ff"
                        FontSize="Medium"
                        FontAttributes="Bold"
                        BackgroundColor="White" 
                        CornerRadius="15"
                        WidthRequest="120"
                        BorderWidth="3"
                        Clicked="After_sca_btn_Clicked"/>
            </StackLayout>

            <ListView x:Name="resultsListView" Grid.Row="1"
                      VerticalOptions="CenterAndExpand">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextCell  Text="{Binding TagName}"
                                  Detail="{Binding Probability, StringFormat='{0:P}'}"
                                  />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

        </Grid>
    </ContentPage.Content>
</ContentPage>
