<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage Icon="wineicon.png"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
    xmlns:skia="clr-namespace:SkiaSharp.Views.Forms;assembly=SkiaSharp.Views.Forms"
             x:Class="SommeliAr.Views.Menu.ScanPage">


    <Grid>
        <Grid.RowDefinitions>
        
            <RowDefinition Height="660"/>
            <RowDefinition Height="8*"/>
            <RowDefinition Height="2*"/>

        </Grid.RowDefinitions>

        <Image x:Name="resultImage" IsVisible="True" Grid.Row="0"/>

        <skia:SKCanvasView x:Name="ImageCanvas"  PaintSurface="OnCanvasViewPaintSurface"
                           Grid.Row="0" Margin="0,40,0,0"/>

        <StackLayout  Grid.Row="1" VerticalOptions="Start" HorizontalOptions="Center" Margin="0,-630,0,0">
                <Label Text="Let's scan bottles!"
                       TextColor="#b794ff"
                       FontSize="Title"
                       FontAttributes="Bold"
                       />
        </StackLayout>


        <StackLayout  VerticalOptions="StartAndExpand" HorizontalOptions="CenterAndExpand" Grid.Row="2" Margin="0,-60,0,0">
            <Button x:Name="Scan_btn" Text="SCAN"
                        TextColor="White"
                        FontSize="Large"
                        FontAttributes="Bold"
                        CharacterSpacing="1"
                        BackgroundColor="#8b52ff"
                        BorderWidth="1"
                        CornerRadius="10"
                        HorizontalOptions="FillAndExpand"
                        Clicked="Scan_btn_Clicked"/>

            </StackLayout>

            <StackLayout VerticalOptions="Start" HorizontalOptions="EndAndExpand" Grid.Row="2" Margin="0,-60,0,0">
                <Button x:Name="After_sca_btn" Text="Result list"
                        BorderColor="#8b52ff"
                        TextColor="#8b52ff"
                        FontSize="Medium"
                        Opacity="0.9"
                        FontAttributes="Bold"
                        BackgroundColor="White" 
                        CornerRadius="15"
                        WidthRequest="120"
                        BorderWidth="3"
                        Clicked="After_sca_btn_Clicked"/>
            </StackLayout>

            <ListView x:Name="resultsListView" VerticalOptions="CenterAndExpand" HorizontalOptions="CenterAndExpand" IsVisible="False">
                <ListView.ItemTemplate>
                    <DataTemplate >
                        <TextCell  Text="{Binding TagName}"
                                  Detail="{Binding Probability}"
                                  />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>

    </Grid>

</ContentPage>
