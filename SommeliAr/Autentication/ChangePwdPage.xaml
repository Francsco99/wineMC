<?xml version="1.0" encoding="UTF-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    x:Class="SommeliAr.Autentication.ChangePwdPage">
   <ContentPage.Content>
       <Grid>
           <Grid.RowDefinitions>
               <RowDefinition Height="Auto"/>
               <RowDefinition Height="Auto"/>
               <RowDefinition Height="Auto"/>
               <RowDefinition Height="Auto"/>
           </Grid.RowDefinitions>

           <StackLayout Grid.Row="0" >
                <Entry x:Name="email_entry" Placeholder="Email" HorizontalOptions="FillAndExpand" />
           </StackLayout>

           <StackLayout Grid.Row="1">
                <Entry x:Name="old_pwd_entry" Placeholder="Old password" HorizontalOptions="FillAndExpand"/>
           </StackLayout>

           <StackLayout Grid.Row="2">
               <Entry x:Name="new_pwd_entry" Placeholder="New password" HorizontalOptions="FillAndExpand"/>

               <Button x:Name="change_pwd_btn" VerticalOptions="Start"
                        Text="Change Password" Clicked="change_pwd_btn_Clicked"/>
           </StackLayout>

            
       </Grid>
            
    </ContentPage.Content>
</ContentPage>
